"""Shared test scaffolding (benchmark harness, reporter, mocks).

These modules are imported from ``tests/{unit,integration,api,performance,latency}``
to keep test code focused on behaviour rather than plumbing.
"""
